[package]
name = "ai-query-check"
version = "0.1.0"
edition = "2021"
description = "Claude Code hook to auto-approve read-only SQL queries"

[dependencies]
brush-parser = "0.3"
sqlparser = "0.51"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
